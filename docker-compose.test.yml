# This file is auto-synced from product-os/jellyfish-config/sync/docker-compose.test.yml
# and should only be edited there!

version: '2.1'

services:
  sut:
    image: balena/jellyfish-core_sut:latest
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - postgres
      - redis
    environment:
      - DATABASE=postgres
      - LOGLEVEL=crit
      - NODE_ENV=test
      - POSTGRES_DATABASE=jellyfish
      - POSTGRES_HOST=postgres
      - POSTGRES_PASSWORD=docker
      - POSTGRES_USER=docker
      - REDIS_HOST=redis
      - REDIS_PASSWORD=
      - REDIS_PORT=6379
      - OAUTH_REDIRECT_BASE_URL=https://jel.ly.fish
      - INTEGRATION_DEFAULT_USER=admin
      - CI=1
    env_file:
      - test.env
    networks:
      - internal
