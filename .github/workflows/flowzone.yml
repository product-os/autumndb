name: Flowzone

on:
  pull_request:
    types: [opened, synchronize, closed]
    branches:
      - "master"

jobs:
  flowzone:
    name: Flowzone
    uses: product-os/flowzone/.github/workflows/flowzone.yml@v0.36.4
    secrets: inherit
